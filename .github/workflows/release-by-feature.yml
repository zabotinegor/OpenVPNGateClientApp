name: Beta Release from feature branches

on:
  push:
    branches:
      - 'feature/**'
      - 'feat/**'
  workflow_dispatch:

jobs:
  build:
    name: Beta Build & Release
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3 # Рекомендуется обновить до v4, если возможно
      with:
        fetch-depth: 0 # Важно для получения всех тегов
        fetch-tags: true # Явно указываем загрузку тегов

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.x'

    - name: Install MAUI workload
      run: dotnet workload install maui

    - name: Generate beta version
      id: versioning
      shell: bash
      run: |
        set -e # Останавливаемся при ошибках (кроме тех, что обработаны || true)
        # set -e -o pipefail # Можно раскомментировать для более строгой проверки пайплайнов

        BRANCH="${GITHUB_REF##*/}"
        SAFE_BRANCH=$(echo "$BRANCH" | tr '/' '-' | tr '_' '-')
        echo "Branch: $BRANCH, Safe Branch: $SAFE_BRANCH"

        echo "🔍 Getting latest stable tag (v8.* excluding beta)..."
        # Добавляем || true, чтобы команда не прерывала скрипт, если теги не найдены
        STABLE_TAG=$(git tag --list 'v8.*' | grep -v 'beta' | sort -Vr | head -n 1) || true
        echo "Latest stable tag found: [$STABLE_TAG]" # [] помогут увидеть, если строка пустая

        if [[ -z "$STABLE_TAG" ]]; then
          echo "No stable tag found, starting with 8.1.0."
          VERSION="8.1.0"
          PATCH=0 # Явно устанавливаем патч в 0 для начальной версии
        else
          # Убираем 'v' и парсим версию
          IFS='.' read -r MAJOR MINOR PATCH <<< "${STABLE_TAG#v}"
          echo "Parsed stable tag: MAJOR=$MAJOR, MINOR=$MINOR, PATCH=$PATCH"
          # Увеличиваем патч для новой версии
          PATCH=$((PATCH + 1))
          VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "Calculated next version base: $VERSION"
        fi

        echo "🔍 Getting latest beta tag for v${VERSION}-${SAFE_BRANCH}-beta.* ..."
        # Добавляем || true и здесь, т.к. для новой ВЕРСИИ может не быть бета-тегов
        BETA_TAG=$(git tag --list "v${VERSION}-${SAFE_BRANCH}-beta.*" | sort -Vr | head -n 1) || true
        echo "Latest beta tag found for this version/branch: [$BETA_TAG]"

        if [[ -n "$BETA_TAG" ]]; then
          # Пытаемся извлечь номер беты (число после 'beta.')
          BETA_NUM_MATCH=$(echo "$BETA_TAG" | grep -oE 'beta\.[0-9]+')
          if [[ -n "$BETA_NUM_MATCH" ]]; then
              BETA_NUM=$(echo "$BETA_NUM_MATCH" | cut -d. -f2)
              echo "Found existing beta number: $BETA_NUM"
              BETA_NUM=$((BETA_NUM + 1))
          else
              echo "Warning: Found beta tag [$BETA_TAG] but couldn't extract beta number. Starting beta from 1."
              BETA_NUM=1
          fi
        else
          echo "No existing beta tag found for v${VERSION}-${SAFE_BRANCH}. Starting beta from 1."
          BETA_NUM=1
        fi
        echo "Next beta number: $BETA_NUM"

        # Собираем финальный тег и версию для приложения
        FINAL_TAG="v${VERSION}-${SAFE_BRANCH}-beta.${BETA_NUM}-auto"
        APP_VERSION="${VERSION}-${SAFE_BRANCH}-beta.${BETA_NUM}" # Версия без суффикса -auto

        echo "Final tag to create: $FINAL_TAG"
        echo "App version for build: $APP_VERSION"

        # Записываем переменные для использования в следующих шагах
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "patch=$PATCH" >> $GITHUB_OUTPUT # Этот patch относится к базовой версии VERSION
        echo "tag=$FINAL_TAG" >> $GITHUB_OUTPUT
        echo "safe_branch=$SAFE_BRANCH" >> $GITHUB_OUTPUT
        echo "app_version=${APP_VERSION}" >> $GITHUB_OUTPUT

    - name: Restore dependencies
      run: dotnet restore ./src/OpenVPNClientApp.sln

    - name: Build APK
      shell: bash # Используем bash для единообразия с versioning и rename
      run: |
        echo "Starting build with AppDisplayVersion=${{ steps.versioning.outputs.app_version }} and ApplicationVersion=${{ steps.versioning.outputs.patch }}"
        dotnet publish ./src/OpenVPNClientApp.sln \
          -f:net8.0-android \
          -c:Release \
          -p:ApplicationDisplayVersion=${{ steps.versioning.outputs.app_version }} \
          -p:ApplicationVersion=${{ github.run_number }} \
          -o ./output

    - name: Rename APK
      shell: bash
      run: |
        VERSION=${{ steps.versioning.outputs.app_version }}
        echo "Looking for APK in ./output for version $VERSION"
        # Ищем APK, который был подписан (обычно содержит -Signed)
        APK_PATH=$(find ./output -name '*-Signed.apk' -print -quit) # -print -quit для отладки и выхода после первого найденного
        if [[ -z "$APK_PATH" ]]; then
          echo "Error: Signed APK not found in ./output"
          # Попробуем найти любой APK на всякий случай
          APK_PATH=$(find ./output -name '*.apk' -print -quit)
          if [[ -z "$APK_PATH" ]]; then
            echo "Error: No APK found in ./output at all!"
            exit 1
          else
            echo "Warning: Found an unsigned APK: $APK_PATH"
          fi
        fi
        NEW_NAME="./output/OpenVPNClient_${VERSION}.apk"
        echo "Renaming '$APK_PATH' to '$NEW_NAME'"
        mv "$APK_PATH" "$NEW_NAME"

    - name: Create beta tag in Git
      # Используем actions/github-script@v6 или v7 для актуальности
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GH_PAT }} # Убедитесь, что GH_PAT имеет права на запись в репозиторий
        script: |
          const tag = '${{ steps.versioning.outputs.tag }}';
          const sha = context.sha;
          console.log(`Attempting to create tag: ${tag} at SHA: ${sha}`);
          try {
            await github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: `refs/tags/${tag}`,
              sha: sha
            });
            console.log(`Successfully created tag ${tag}`);
          } catch (error) {
            // Код 422 означает, что ссылка (тег) уже существует
            if (error.status === 422) {
              console.log(`⚠️ Tag ${tag} already exists. Skipping tag creation.`);
            } else {
              console.error(`❌ Failed to create tag ${tag}:`, error);
              throw error; // Перебрасываем ошибку, чтобы остановить workflow, если это не ошибка 422
            }
          }

    - name: Upload APK to GitHub Releases (Beta)
      # Используем ncipollo/release-action@v1 - она все еще актуальна
      uses: ncipollo/release-action@v1
      with:
        artifacts: ./output/OpenVPNClient_*.apk # Ищем переименованный APK
        token: ${{ secrets.GH_PAT }} # Убедитесь, что GH_PAT имеет права на создание/обновление релизов
        tag: ${{ steps.versioning.outputs.tag }} # Тег для релиза = созданный Git-тег
        name: "Beta Release ${{ steps.versioning.outputs.tag }}" # Имя релиза
        body: | # Используем | для многострочного описания
          Автоматическая сборка APK из ветки `${{ github.ref_name }}`.
          Коммит: ${{ github.sha }}
          Версия приложения: ${{ steps.versioning.outputs.app_version }}
        prerelease: true # Отмечаем как pre-release
        allowUpdates: true # Разрешаем обновлять существующий релиз с этим тегом
        replacesArtifacts: true # Заменяем артефакты, если релиз обновляется
        # generateReleaseNotes: true # Можно включить, если хотите авто-генерацию заметок на основе коммитов