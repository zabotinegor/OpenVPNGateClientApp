---
description: "Mirror-sync agent/skill/tool assets from CopilotTools into target repository."
agent: agent-sync
---

Load `.github/skills/agent-sync/SKILL.md` and follow its workflow.

Run the full `.github/scripts/sync-agent-assets.ps1` sync. Do not limit the run to `.opencode` and do not report completion until the managed `.gitignore` block has been rebuilt and verified for the synced OpenCode files.

$ARGUMENTS
