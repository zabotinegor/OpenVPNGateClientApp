# Claude adapter: agent-sync

The canonical workflow is .github/skills/agent-sync/SKILL.md.
Load that skill and follow it as the sole semantic authority. This file only provides the Claude command entrypoint; runtime-native permissions and invocation remain outside the canonical workflow.

Load AGENTS.md first.
